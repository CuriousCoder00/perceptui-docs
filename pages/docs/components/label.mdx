import { Label } from "@perceptui/lib";
import { IssueHeader } from "@/components/issue-header";
import { Info } from "lucide-react";

<h1 className="text-4xl font-bold mt-5">Label</h1>
<p className="text-xl">Renders an accessible label associated with controls.</p>
<IssueHeader source="label" />

Start using via importing:

```tsx code copy
import { Label } from "@perceptui/lib";
```

<div className="flex flex-col my-6 w-full">
  <div className="flex flex-col w-full gap-2 p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Label>This is the label</Label>
  </div>
  <div className="block w-full py-0">
    ```jsx code copy
    <Label>This is the label</Label>
    ```
  </div>
</div>

## API References

| Prop      | Type                                     | Description         | Default |
| :-------- | :--------------------------------------- | :------------------ | :------ |
| className | `string`                                 | className           | \_      |
