import { Badge } from "@perceptui/lib";
import { IssueHeader } from "@/components/issue-header";
import { Info } from "lucide-react";

<h1 className="text-4xl font-bold mt-5">Badge</h1>
<p className="text-xl">A styled badge.</p>
<IssueHeader source="badge" />

Start using via importing:

```tsx code copy
import { Badge } from "@perceptui/lib";
```

<div className="flex flex-col my-6">
  <div className="flex gap-2 p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Badge>Badge</Badge>
  </div>
  <div className="block w-full py-0">
    ```jsx code copy
    <Badge>Badge</Badge>
    ```
  </div>
</div>

## API References

| Prop    | Type                                     | Description          | Default |
| :------ | :--------------------------------------- | :------------------- | :------ |
| variant | `"solid" \| "outline" \| "ghost"`        | Form of the Badge   | solid   |
| color   | `string`                                 | Color of the Badge  | blue    |
| radius  | `"none"\|"sm"\|"md"\|"lg"\|"xl"\|"full"` | Radius of the Badge | none      |
| size    | `"xs"\|"sm"\|"md"\|"lg"\|"xl"`           | Size of the Badge   | xs      |

## Examples

### Variant

Use the `variant` prop to control the visual style of the Badge.

<div className="flex flex-col my-6 px-2">
  <div className="flex gap-2 p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Badge variant="solid">Badge</Badge>
    <Badge variant="outline">Badge</Badge>
    <Badge variant="ghost">Badge</Badge>
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge variant="solid">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge variant="outline">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge variant="ghost">Badge</Badge>
    ```
  </div>
</div>

### Color

Use the `color` prop to control the color of the Badge.

<div className="flex flex-col my-6 px-2">
  <div className="flex flex-wrap gap-2 p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Badge color="blue">Badge</Badge>
    <Badge color="emerald">Badge</Badge>
    <Badge color="gray">Badge</Badge>
    <Badge color="rose">Badge</Badge>
    <Badge color="cyan">Badge</Badge>
    <Badge color="yellow">Badge</Badge>
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge color="blue">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge color="emerald">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge color="gray">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge color="rose">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge color="cyan">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge color="yellow">Badge</Badge>
    ```
  </div>
</div>

<div className="flex justify-start items-start bg-cyan-500/10 w-full pl-6 p-2 rounded-xl py-4">
  <Info className="inline" /> There are more color properties. Take your time to
  explore
</div>

### Radius

Use the `radius` prop to assign a specific radius value.

<div className="flex flex-col my-6 px-2">
  <div className="flex gap-2 p-4 bg-blue-500/15 mb-2 rounded-lg">
    <Badge radius="none">Badge</Badge>
    <Badge radius="sm">Badge</Badge>
    <Badge radius="md">Badge</Badge>
    <Badge radius="lg">Badge</Badge>
    <Badge radius="xl">Badge</Badge>
    <Badge radius="full">Badge</Badge>
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge radius="none">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge radius="sm">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge radius="md">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge radius="lg">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge radius="xl">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge radius="full">Badge</Badge>
    ```
  </div>
</div>

### Size

Use the `size` prop to assign a specific size value.

<div className="flex flex-col my-6 px-2">
  <div className="flex items-end gap-2 p-4 bg-blue-500/15 mb-2 rounded-lg">
  <div><Badge size="xs">Badge</Badge></div>
  <div><Badge size="sm">Badge</Badge></div>
  <div><Badge size="md">Badge</Badge></div>
  <div><Badge size="lg">Badge</Badge></div>
  <div><Badge size="xl">Badge</Badge></div>
  </div>

  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge size="xs">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge size="sm">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge size="md">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge size="lg">Badge</Badge>
    ```
  </div>
  <div className="block text-wrap w-full py-1">
    ```jsx code copy
    <Badge size="xl">Badge</Badge>
    ```
  </div>
</div>
